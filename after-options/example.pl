#! /usr/bin/env perl

use strict;
use warnings;

# Example for showing :make
# :make
# :cn jump to next error
# :cp jump to previous error
# :bc jump to cope window, or open if closed

$foo = 'Hello';

say $bar;

my %hash = {
    key1 => 'value1',
    key 2 => 'value2'
    'key 3' => 'value3',
};

